# User ver 3
version: '3'

services:
  web:
    # Build from default dockerfile
    build: .
    image: loitd/nginx-php7:dev
    # Command to execute
    # command: python manage.py runserver 0.0.0.0:8000
    # command: /opt/loitd/start.sh
    # Overwrite entrypoint if need
    # entrypoint: [php, -d, vendor/bin/phpunit]
    volumes:
      - ./html:/var/www/html
    ports:
      - "80:80"
    # depends_on:
    #   - db